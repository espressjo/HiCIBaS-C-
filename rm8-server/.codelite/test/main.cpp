#include <stdio.h>
#include <string>
#include <iostream>
#include <cmath>
using namespace std;
int main(int argc, char **argv)
{
	int mst =pow(2,3);
	
	cout<<(((mst & pow(2,3)==pow(2,3)) && ((mst & pow(2,3))==pow(2,3)))<<endl;
	//cout<<((mst & pow(2,0) == pow(2,0)) || ((mst & pow(2,1)) == pow(2,1)) || ((mst & pow(2,2)) == pow(2,2))<<endl;
	return 0;
}
